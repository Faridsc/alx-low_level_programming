/**
 * _strpbrk - locates the first occurrence in the string s of any
 *  of the bytes in the string accept
 * @s: the source string
 * @accept: the string to locate
 * Return: the first accurence char*
 */
char *_strpbrk(char *s, char *accept)
{
	return (accept);
}