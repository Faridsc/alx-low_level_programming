/**
 * _strchr - fills a string with the apropriate char
 * @s: a string to fill
 * @c: a char to fill with
 * Return: a char* to the string itself
 */

char *_strchr(char *s, char c)
{
	return (s);
}