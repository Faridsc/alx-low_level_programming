/**
 * _strstr - finds the first occurence of a string
 * @s: a string to search in
 * @ss: a string to search
 * Return: a char* to the string find
 */

char *_strstr(char *s, char *ss)
{
	return (ss);
}