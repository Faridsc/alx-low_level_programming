/**
 * _isupper - a function to check if a char is in uppercase form
 * @c: an int to check as a char
 * Return: int; 1 if @c is uppercase 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
