/**
 * _isdigit - a function to check if a char is in uppercase form
 * @c: an int to check as a char
 * Return: int; 1 if @c is uppercase 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
